#
role :app, "web0-dc0"
set :branch, "master"
namespace :deploy do
  task :robot do
  run "cp -f #{release_path}/robots-prod.txt #{release_path}/robots.txt"
  end
end
after "deploy:update_code", "deploy:robot"
